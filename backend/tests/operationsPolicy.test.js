const test=require('node:test');const assert=require('node:assert/strict');const {assessObservation,assertTransition}=require('../domain/operationsPolicy');
test('safety assessment is deterministic and provenance remains attached',()=>{const r=assessObservation({sourceEventId:'offline-1',penId:'p1',metric:'dissolved_oxygen',value:3.8,unit:'mg/L',observedAt:'2026-07-18T12:00:00Z',provenance:{device:'d1'}});assert.equal(r.severity,'high');assert.deepEqual(r.reasons,['below_safe_minimum']);assert.equal(r.normalized.provenance.device,'d1');});
test('operator cannot self-approve safety action',()=>{assert.throws(()=>assertTransition('triaged','approved',{role:'viewer',approvalNote:'reviewed against site SOP'}),/Manager/);assert.doesNotThrow(()=>assertTransition('triaged','approved',{role:'manager',approvalNote:'reviewed against site SOP'}));});
