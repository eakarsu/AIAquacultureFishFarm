{
  "name": "ai-aquaculture-fish-farm",
  "version": "1.0.0",
  "description": "AI-powered net-pen aquaculture and fish farming operations platform (farms, pens, feed, treatments, harvests, sea lice, biomass)",
  "private": true,
  "scripts": {
    "start": "bash start.sh",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "seed": "cd backend && node seed/seed.js",
    "migrate": "bash scripts/migrate.sh",
    "create-admin": "cd backend && npm run create-admin",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  }
}
